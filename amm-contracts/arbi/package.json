{
  "name": "simple-amm-arbi",
  "version": "1.0.0",
  "description": "Minimal AMM pool for WETH/USDC on Arbitrum Sepolia",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network arbitrumSepolia",
    "add-liquidity": "hardhat run scripts/addLiquidity.js --network arbitrumSepolia",
    "test-swap": "hardhat run scripts/testSwap.js --network arbitrumSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  }
}
